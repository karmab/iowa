---

- name: Set Test Variables
  set_fact:
    group: designate
    host: "{{ designate_host }}"
    ports: 
     - 9001
    packages: "{{ designate_packages }}"
    services: "{{ designate_services }}"
    configuration_files:
     - '/etc/designate/designate.conf'

- include: common.yml
