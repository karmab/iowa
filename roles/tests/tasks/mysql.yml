---

- name: Set Test Variables
  set_fact:
    host: "{{ mysql_host }}"
    ports: 
     - 3306
    packages: "{{ mysql_packages }}"
    services:
     - 'mariadb'
    configuration_files:
     - '/etc/my.cnf'

- include: common.yml
