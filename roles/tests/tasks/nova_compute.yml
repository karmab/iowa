---

- name: Set Test Variables
  set_fact:
    packages: "{{ nova_compute_packages }}"
    services:
     - 'openstack-nova-compute'
    configuration_files:
     - '/etc/nova/nova.conf'

- include: common.yml
