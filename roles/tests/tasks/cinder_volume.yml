---

- name: Set Test Variables
  set_fact:
    group: cinder
    packages: "{{ cinder_packages }}"
    services: 
     - "{{ cinder_volume_service }}"
    configuration_files:
     - '/etc/cinder/cinder.conf'

- include: common.yml
