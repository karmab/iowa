---

- name: Set Test Variables
  set_fact:
    group: cinder
    packages: "{{ cinder_packages }}"
    services:
     - 'openstack-cinder-volume'
    configuration_files:
     - '/etc/cinder/cinder.conf'

- include: common.yml
