---

- name: Set Test Variables
  set_fact:
    host: "{{ glance_host }}"
    ports: 
     - 9292
    packages: "{{ glance_packages }}"
    services: "{{ glance_services }}"
    configuration_files:
     - '/etc/glance/glance-api.conf'
     - '/etc/glance/glance-registry.conf'

- include: common.yml
