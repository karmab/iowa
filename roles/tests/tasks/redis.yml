---

- name: Set Test Variables
  set_fact:
    host: "{{ redis_host }}"
    ports: 
     - 6379
    packages: "{{ redis_packages }}"
    services:
     - 'redis'
    configuration_files:
     - '/etc/redis.conf'

- include: common.yml
