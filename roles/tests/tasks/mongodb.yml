---

- name: Set Test Variables
  set_fact:
    host: "{{ mongodb_host }}"
    ports: 
     - 27017
    packages: "{{ mongodb_packages }}"
    services:
     - '{{ mongodb_service }}'
    configuration_files:
     - '/etc/mongod.conf'

- include: common.yml
