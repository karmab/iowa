---

- name: Set Test Variables
  set_fact:
    group: nova
    host: "{{ nova_host }}"
    ports: 
     - 8774
    packages: "{{ nova_packages }}"
    services: "{{ nova_services }}"
    configuration_files:
     - '/etc/nova/nova.conf'

- include: common.yml
