---

- name: Set Test Variables
  set_fact:
    group: heat
    host: "{{ heat_host }}"
    ports: 
     - 8000
     - 8004
    packages: "{{ heat_packages }}"
    services: "{{ heat_services }}"
    configuration_files:
     - '/etc/heat/heat.conf'

- include: common.yml
