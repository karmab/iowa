---

- name: Set Test Variables
  set_fact:
    group: swift
    host: "127.0.0.1"
    ports: 
     - 6200
     - 6201
     - 6202
    packages: "{{ swift_storage_packages }}"
    services: "{{ swift_storage_services }}"
    configuration_files:
     - '/etc/swift/swift.conf'
     - '/etc/swift/object.builder'
     - '/etc/swift/container.builder'
     - '/etc/swift/account.builder'

- include: common.yml
